---
'@totoday/quinn-sdk': minor
---

Replace the old `mutationAccess` SDK config with `allowQuinnMutation`, rename the public mutation error to `QuinnMutationGuardError`, and switch the runtime environment flag to `QUINN_ALLOW_QUINN_MUTATION`.
