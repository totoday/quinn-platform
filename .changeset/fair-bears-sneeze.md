---
'@totoday/quinn-sdk': minor
---

Align SDK list pagination and media field semantics with the latest platform contract.

Groups and roles list endpoints now return paged results. Organization and course media references now use `mediaId`, and course assigned members no longer expose `status`.
